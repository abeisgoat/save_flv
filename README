This script locates chromium's PID and finds the detached file handlers which Flash uses to hide temporary files then saves that detached file to a file for playback later. Make sure to let the video buffer completely before running!
